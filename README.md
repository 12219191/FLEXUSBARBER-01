flexusbarber/
├── .lovable/      # Lovable.dev artifacts (AI-generated starter)
├── public/        # Static assets (logos, images)
├── src/           # Core app (components, pages, hooks)
├── supabase/      # DB schemas, migrations, RLS policies
├── .env           # Secrets (SUPABASE_URL, STRIPE_KEY - gitignore!)
├── package.json   # Dependencies (vite, @supabase/supabase-js, lucide-react)
├── vite.config.ts # Build config
├── tailwind.config.ts # Themes (custom palettes per tenant)
└── ... (configs for TS, ESLint, testing)# FLEXUSBARBER-01
